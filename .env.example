# Copy this file to `.env` and fill in the real values.
# `.env` is git-ignored — never commit real credentials.
# On Streamlit Cloud, set these two keys under Settings → Secrets instead.

MONGO_URI=mongodb+srv://<user>:<password>@suretydb.6jj3uxh.mongodb.net/?appName=SuretyDB
MONGO_DB=surety_dashboard_summary
